function User(firstName, location, language, unit, position) {
  this.firstName = firstName;
  this.location = location;
  this.language = language;
  this.unit = unit;
  // this.position = position;
}

module.exports = User;
